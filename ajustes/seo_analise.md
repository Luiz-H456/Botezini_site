# Análise de SEO - index.html

1. **Meta tags existentes:**
   - `charset`
   - `viewport`
   - `title`
   - `description`
   - `canonical`

2. **Tags críticas faltando:**
   - `og:*` (todas as tags de Open Graph: `og:title`, `og:description`, `og:image`, `og:url`, `og:type`, `og:site_name`, `og:locale`)
   - `twitter:*` (todas as tags de Twitter Cards: `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`)
   - `robots` (meta tag `index, follow`)

3. **Schema JSON-LD:**
   - Não há nenhum schema JSON-LD presente no arquivo.

4. **Links para sitemap ou robots.txt:**
   - Não há links para `sitemap.xml` ou `robots.txt` no HTML.
